Blackhole
----------------------

Make sure your system has the Qt6 and Qt5-compat libraries installed with your package manager.
On Arch Linux, the packages can be installed with the following command:
```
sudo pacman -S qt6 qt6-5compat
```

-- Build instructions --
```
cd build
cmake ..
make
```
./blackhole
